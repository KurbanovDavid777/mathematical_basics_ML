# Laboratory Work 1 — Matrix Decomposition Algorithms. PCA
# Лабораторная работа 1 — Алгоритмы разложения матриц. PCA

---

## 🇷🇺 Описание лабораторной работы

### Название
**Лабораторная работа 1: Алгоритмы разложения матриц. Метод главных компонент (PCA)**

### Цель работы
Изучить методы матричного разложения и их применение в задачах анализа данных, в частности метод главных компонент (PCA) и Kernel PCA, для снижения размерности данных и решения задач классификации.

### Краткое описание
В данной лабораторной работе рассматривается применение методов матричного разложения для анализа данных. Основной акцент сделан на методе главных компонент (PCA), который используется для снижения размерности данных путем линейного преобразования признаков в новое пространство, максимально сохраняющее дисперсию данных.

Дополнительно предлагается реализовать метод **Kernel PCA** для решения задачи классификации линейно-неразделимых данных.

---

##  Теоретическая часть

Пусть задана матрица входных данных:

\[
X = (x_1, x_2, \dots, x_n)^T
\]

где каждый вектор \( x_i \in \mathbb{R}^m \) описывает один объект. Перед применением PCA данные должны быть **центрированы по каждому признаку**.

Линейное преобразование данных определяется следующим образом:

\[
t_i = x_i W
\]

где:
- \( W \in \mathbb{R}^{m \times s} \) — матрица главных компонент,
- \( s \) — число выбранных компонент.

Матрица \( W \) формируется из собственных векторов матрицы ковариации \( X^T X \). Вклад каждой компоненты в объясняемую дисперсию пропорционален соответствующему сингулярному числу.

---

##  Сингулярное разложение

Сингулярное разложение матрицы данных:

\[
X = U \Sigma V^T
\]

где:
- \( U \) — матрица левых сингулярных векторов,
- \( \Sigma \) — диагональная матрица сингулярных чисел,
- \( V \) — матрица правых сингулярных векторов.

Сингулярные числа определяются через собственные значения матрицы \( XX^T \):

\[
\sigma_i = \sqrt{\lambda_i(XX^T)}
\]

---

##  QR-алгоритм

Для вычисления собственных значений и векторов используется **QR-алгоритм**, основанный на последовательном применении QR-разложения:

\[
A = QR
\]

QR-разложение реализуется с использованием модифицированного алгоритма ортогонализации Грама–Шмидта.

---

##  Задания

### 1. Метод главных компонент (PCA)

1. Реализовать сингулярное разложение матрицы **без использования**:
   - `numpy.linalg.svd`
   - `numpy.linalg.eig`

   Рекомендуется использовать **QR-алгоритм**.

2. Выбрать задачу машинного обучения на размеченной выборке:
   - не менее **5 признаков**;
   - модель: **искусственная нейронная сеть**.

3. Использовать реализованное SVD для:
   - получения матрицы главных компонент;
   - вычисления объясняемой дисперсии.

4. Определить достаточное число главных компонент:
   - визуализировать данные;
   - отобразить распределение целевой переменной в пространстве компонент;
   - проанализировать физический или статистический смысл компонент (если применимо).

5. Оценить:
   - изменение качества модели;
   - изменение времени обучения после применения PCA.

6. *(Дополнительно)* Реализовать ускоренные методы SVD с меньшей вычислительной сложностью.

---

### 2. Kernel PCA

1. Выбрать линейно-неразделимый набор данных для классификации.
2. Реализовать вычисление ядровых матриц для:
   - полиномиального ядра;
   - RBF-ядра;
   - сигмоидального ядра.
3. Выполнить спектральное разложение ядровой матрицы.
4. Сравнить PCA и Kernel PCA:
   - визуализация проекций;
   - проверка линейной разделимости.
5. Оценить изменение метрик качества классификации:
   - на исходных данных;
   - на данных после Kernel PCA.

---

## 🇬🇧 Task Description (English Version)

### Title
**Laboratory Work 1: Matrix Decomposition Algorithms. Principal Component Analysis (PCA)**

### Objective
To study matrix decomposition techniques and their application to data analysis tasks, including Principal Component Analysis (PCA) and Kernel PCA for dimensionality reduction and classification.

### Description
This laboratory focuses on applying matrix decomposition methods to analyze data. The core method is PCA, which reduces dimensionality by projecting data into a new coordinate system that preserves the maximum variance.

An additional task involves implementing **Kernel PCA** for classifying non-linearly separable datasets.

---

##  Tasks

### 1. Principal Component Analysis (PCA)

1. Implement Singular Value Decomposition **from scratch**, without using:
   - `numpy.linalg.svd`
   - `numpy.linalg.eig`

   The **QR algorithm** is recommended.

2. Select a labeled machine learning dataset:
   - at least **5 features**;
   - model: **artificial neural network**.

3. Use the implemented SVD to:
   - compute principal components;
   - calculate explained variance.

4. Determine an appropriate number of components:
   - visualize the transformed data;
   - analyze target distribution in component space;
   - interpret components if possible.

5. Evaluate:
   - model performance changes;
   - training time differences after PCA.

6. *(Optional)* Implement computationally efficient SVD variants.

---

### 2. Kernel PCA

1. Select a non-linearly separable classification dataset.
2. Implement kernel matrices for:
   - polynomial kernel;
   - radial basis function (RBF) kernel;
   - sigmoid kernel.
3. Perform spectral decomposition of the kernel matrix.
4. Compare PCA and Kernel PCA:
   - projection visualization;
   - linear separability analysis.
5. Evaluate classification metrics:
   - on original data;
   - on Kernel PCA-transformed data.

---

##  Expected Deliverables

- Jupyter Notebook with full implementation.
- Visualizations and explanations.
- Conclusions on PCA and Kernel PCA effectiveness.
